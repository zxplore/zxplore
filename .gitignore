*.tmp
*.log
/dist/
/*.tar.gz
.DS_Store
__pycache__/
*.pyc

# Go build
/zxplore-bin
/zxplore
/zxplore-tui
*.o

# rendered icon rasters
assets/*.png

# git bundles
*.bundle

# local working notes — never committed
/[C]LAUDE.md
[C]LAUDE.md.bak.*
.buildnum
.testtmp/
/zxplore-api
/zxplore-txn

# Agent rules live in the claude-brain repo and are laid down locally by its
# restore.sh. They must never be committed to a public repo — vmxplore already
# ignores them; this keeps the three consoles consistent.
/CLAUDE.md
CLAUDE.md.bak.*
